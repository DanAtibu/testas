on:
  push:
jobs:
   build: 
     runs-on: ubuntu-latest
     steps: 
      - uses: actions/checkout@v3
        with: 
          fetch-depth: 0
      - name: get file changes
        run: |
          echo '$github'
          echo '$secrets'


      - name: display zith run
        run: env
          
# git diff --name-only ${{ github.event.before }} ${{ github.event.after }}
